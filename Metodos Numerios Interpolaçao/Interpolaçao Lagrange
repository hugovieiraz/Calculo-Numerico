import numpy as np

n = 4
X = np.array([1.20, 1.35, 1.65, 1.80])
Y = np.array([0.1823, 0.3001, 0.5008, 0.5878])
x = 1.75
p = 0

for i in range(0, n):
    L = 1
    for j in range(0, n):
        if i != j:
            L = L * (x - X[j]) / (X[i] - X[j])
    p = p + Y[i] * L

print('p(x) = %.7f' % p)
